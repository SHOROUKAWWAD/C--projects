#include <stdio.h>
  int main() {

  double result; //we defined the variable result from type double that allows decimals
  result= (3.0+1.0)/5.0;
  printf("the value of 4.0/5.0 is %lf\n ", result);
  return 0;
  }
