


#include <stdio.h>


int main()
{

double a ;
double I ;
double M ;
double A;
double R ;
scanf( "%lf", &a) ;
scanf( "%lf", &I) ;
 M= a+I;
  printf(" the sum of C&I= %f\n", M);
A = a-I;
 printf(" the difference of the two doubles%f", A);
R = a*a;
 printf("the square of C %f\n", R);


 int G;
 int F;
 int H ;
 int Z ;
 scanf("%d",&G);
 scanf("%d",&F );
 H=  G + F;
 Z = G*F ;
  printf(" the sum of the two integers%d", H);
  printf("the product of the two integers %d", Z);

  char S;
  char N;



   scanf (" %c", &S);
   getchar();
   scanf ( "%c",&N );
    getchar();
    int B = S+N;
  int D = S*N;
   printf (" the product of the two chars %d", D); // the product of the two chars
   printf ( " the sum of the two chars %d", B);


 char X=S+N;
 printf( "%c", X)

return 0;
}
